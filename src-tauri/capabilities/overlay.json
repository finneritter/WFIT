{
  "identifier": "overlay",
  "description": "Overlay windows (Cascade HUD pill, relic-crack price box) — read-only displays. They only listen for their push events and invoke read-only fetch commands; all window show/hide/position is done from Rust. NOTE: a window absent from this list gets NO permissions — its webview can't listen/invoke, renders nothing, and the transparent window is invisible.",
  "local": true,
  "windows": ["overlay", "relic-overlay"],
  "permissions": ["core:event:default"]
}
