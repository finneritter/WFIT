# Node / frontend
node_modules/
dist/
*.local
.vite/

# Rust / Tauri
/src-tauri/target/
target/
# Tauri-generated ACL/schema files — regenerated on every build
/src-tauri/gen/
# Mobile icon sets from `tauri icon` — desktop app, not needed
/src-tauri/icons/android/
/src-tauri/icons/ios/

# Local SQLite (app DB lives in $APPDATA at runtime; ignore any stray local copies)
*.db
*.db-shm
*.db-wal
*.sqlite

# Env / secrets
.env
.env.local
.env.*.local

# Personal Claude instructions (not shared)
CLAUDE.local.md

# OS
.DS_Store
Thumbs.db
