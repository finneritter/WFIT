{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git pull --ff-only 2>&1 || echo 'WFIT: git pull failed (offline, dirty tree, or diverged) — sync manually'",
            "timeout": 30,
            "statusMessage": "Pulling latest from main…"
          }
        ]
      }
    ]
  }
}
